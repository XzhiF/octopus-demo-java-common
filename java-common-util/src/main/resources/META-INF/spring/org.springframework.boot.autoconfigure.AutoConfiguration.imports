# java-common-util registers no Spring auto-configurations (JwtConfig is a plain
# record, JwtUtil is a static helper). The previously listed JwtAutoConfiguration
# never existed in source — removed to let consumers boot.
